export const reset_action = state => ({
    type: "RESET_ACTION"
});

export const forgot_password = state => ({
    type: "FORGOT_PASSWORD",
    payload: state
});

export const reset_password = state => ({
    type: "RESET_PASSWORD",
    payload: state
});

export const change_password = state => ({
    type: "CHANGE_PASSWORD",
    payload: state
});